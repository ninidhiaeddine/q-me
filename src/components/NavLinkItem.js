class NavLinkItem {
  constructor(isActive, text, href, component) {
    this.isActive = isActive;
    this.text = text;
    this.href = href;
    this.component = component;
  }
}
export default NavLinkItem;
